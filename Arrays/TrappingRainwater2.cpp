#include <bits/stdc++.h>
using namespace std;

int returnWater(vector<int> arr,int n)
{
    if (arr.size() <= 2)
        return 0;
    else
    {
        vector<int> leftMax(n);
        vector<int> rightMax(n);
        for(int i=0;i<n;i++)
        {
            leftMax[i]= max(leftMax[i-1],)
        }
    }
}
int main()
{

    return 0;
}